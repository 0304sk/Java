package Programs;

public class NoofCharInString {
	
	public static void main(String[] args) {
	/*	
		//to find the no of char including space in a string
		String s = "Hello World! Welcome Home";
		char[] c = s.toCharArray();
		int size = c.length;
		System.out.println("The size of the characters in the string is: "+size);
		for (char d : c) {
			
			System.out.print(d);
			
		}      
		
		//no. of upper case, lower case, digits and special char from a string using special methods
		
		String s = "Hello World! Welcome Home..123";
		 int upper = 0 , lower = 0 , digit = 0 , spch = 0, space = 0;
		String u = "" , l = "" , d = "" , sc = "";
		
		for (int i = 0; i < s.length(); i++) {
			
			char a = s.charAt(i);
			
			if (Character.isUpperCase(a)) {
				upper++;
				u = u+a;
			}else if (Character.isLowerCase(a)) {
				lower++;
				l = l+a;	
			}else if (Character.isDigit(a)) {
				digit++;
				d = d+a;
				} else if (Character.isSpaceChar(a)) {
					space++;
					
				}else {
					spch++;
					sc = sc+a;
				}
			
		}
		System.out.println("The total no of char in the string is: " +s.toCharArray().length);
		System.out.println("The no of upper case char in the String is " +upper+ " and they are: " +u );
		System.out.println("The no of lower case char in the String is " +lower+ " and they are: " +l );
		System.out.println("The no of digits in the String is " +digit+ " and they are: " +d );
		System.out.println("The no of special char in the String is " +spch+ " and they are: " +sc );
		System.out.println("The no of space in the String is " +space);
		
		//To find the length of chars in string without space
		String s = "Hello World! Welcome Home..123";
		
		int size = 0;
		String[] split = s.split(" ");
		for (String string : split) {
		
			//System.out.println(string);
			int length = string.length();
			size = length+size;
			System.out.println("The length of the word " +string+ " is " +length);
		}
		System.out.println("Total no of char in the string without space is " +size);
		
		*/
	}

}
